<Page x:Class="MediaCatalog.Views.Pages.PeoplePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#1a1a2e">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header + Search -->
        <Grid Grid.Row="0" Margin="32,28,32,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="People"
                       Foreground="White" FontSize="28" FontWeight="Bold"/>
            <Border Grid.Column="1" Background="#16213e"
                    BorderBrush="#0f3460" BorderThickness="1"
                    CornerRadius="6" Padding="10,6">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔍" Foreground="#6c757d"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Background="Transparent" Foreground="White"
                             BorderThickness="0" FontSize="13" Width="200"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             CaretBrush="White"/>
                    <Button Content="✕" Background="Transparent" BorderThickness="0"
                            Foreground="#6c757d" Cursor="Hand" Padding="4,0"
                            Command="{Binding ClearSearchCommand}"
                            Visibility="{Binding SearchText, Converter={StaticResource StringToVisibility}}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Count -->
        <TextBlock Grid.Row="1" Margin="32,10,32,8" Foreground="#6c757d" FontSize="12">
            <Run Text="{Binding FilteredCount}"/>
            <Run Text=" of "/>
            <Run Text="{Binding TotalCount}"/>
            <Run Text=" people"/>
        </TextBlock>
        <ProgressBar Grid.Row="1" Height="2" VerticalAlignment="Bottom"
                     IsIndeterminate="True" Foreground="#e94560"
                     Background="Transparent" BorderThickness="0"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>

        <!-- Grid -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding People}" Margin="32,0,32,32">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="130" Margin="0,0,14,14"
                                Background="#16213e" CornerRadius="8" Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                    Command="{Binding DataContext.OpenPersonCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}"/>
                            </Border.InputBindings>
                            <StackPanel>
                                <Border CornerRadius="8,8,0,0" Height="160" ClipToBounds="True">
                                    <Grid>
                                        <Rectangle Fill="#0f3460"/>
                                        <TextBlock Text="👤" FontSize="40" Foreground="#16213e"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        <Image Stretch="UniformToFill"
                                               Source="{Binding PhotoPath}"/>
                                    </Grid>
                                </Border>
                                <Border Padding="10,8,10,10">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" Foreground="White"
                                                   FontSize="13" FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding CreditsSummary}"
                                                   Foreground="#6c757d" FontSize="11"
                                                   TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty state -->
        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="{Binding People.Count, Converter={StaticResource ZeroToVisibility}}">
            <TextBlock Text="👤" FontSize="64" HorizontalAlignment="Center"/>
            <TextBlock Text="No people found" Foreground="White"
                       FontSize="20" FontWeight="SemiBold"
                       HorizontalAlignment="Center" Margin="0,16,0,8"/>
            <TextBlock Foreground="#6c757d" FontSize="14" HorizontalAlignment="Center"
                       Text="Cast and crew are populated after a TMDB scan."/>
        </StackPanel>
    </Grid>
</Page>